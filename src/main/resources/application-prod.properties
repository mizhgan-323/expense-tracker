# Production configuration for Railway + Supabase

# Database configuration from environment variables
spring.datasource.url=${https://zjqflqalrypsfkkoowkx.supabase.co}
spring.datasource.username=${mizhgan-323's Project:postgres}
spring.datasource.password=${misasa123}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate configuration
# ?????: ??????????? update ?????? create-drop ? ??????????!
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false

# Server configuration
server.port=${PORT:8080}

# Swagger/OpenAPI Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.try-it-out-enabled=true

# Logging
logging.level.root=INFO
logging.level.money_tracker.expense_tracker=INFO